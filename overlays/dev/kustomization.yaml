apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

images:
  - name: itelleria/demo
    newTag: "4.0"

resources:
  - ../../base

configMapGenerator:
  - name: database
    behavior: merge
    literals:
      - database-url="jdbc:url-to-database-dev"
      - database-username="username-database-dev"

secretGenerator:
  - name: database
    behavior: merge
    literals:
      - database-password="password-database-dev"